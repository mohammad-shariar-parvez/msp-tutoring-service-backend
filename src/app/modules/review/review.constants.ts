export const reviewFilterableFields = ['courseId'];

export const reviewRelationalFileds = ['courseId'];

export const reviewRelationalFiledsMapper: { [key: string]: string; } = {
	courseId: 'course'
};